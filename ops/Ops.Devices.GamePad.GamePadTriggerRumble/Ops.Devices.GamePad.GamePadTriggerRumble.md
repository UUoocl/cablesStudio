Triggers a localized trigger vibration (rumble) effect on compatible gamepads (such as Xbox Wireless Controllers) using the Web Gamepad API.

To use this op, connect the gamepad object output (e.g. from `Ops.Devices.GamePad.GamePads`) to the `GamePad Data` input of this operator.
