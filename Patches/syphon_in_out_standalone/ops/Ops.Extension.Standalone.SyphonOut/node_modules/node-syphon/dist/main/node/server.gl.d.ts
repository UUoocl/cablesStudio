import { SyphonServerDescription } from '../../common/types';

export type SyphonTextureTarget = 'GL_TEXTURE_RECTANGLE_EXT' | 'GL_TEXTURE_2D';
export declare class SyphonOpenGLServer {
    private _server;
    private _dataDeprecationWarned;
    private _handleDeprecationWarned;
    constructor(name: string);
    dispose(): void;
    publishImageData(data: Uint8ClampedArray, textureTarget: SyphonTextureTarget, imageRegion: {
        x: number;
        y: number;
        width: number;
        height: number;
    }, textureDimension: {
        width: number;
        height: number;
    }, flipped: boolean): void;
    publishImageData(data: Uint8ClampedArray, textureTarget: SyphonTextureTarget, imageRegion: {
        x: number;
        y: number;
        width: number;
        height: number;
    }, textureDimension: {
        width: number;
        height: number;
    }, flipped: boolean): void;
    publishSurfaceHandle(handle: Buffer, textureTarget: SyphonTextureTarget, imageRegion: {
        x: number;
        y: number;
        width: number;
        height: number;
    }, textureDimension: {
        width: number;
        height: number;
    }, flipped: boolean): void;
    publishSurfaceHandle(handle: Buffer, imageRegion: {
        x: number;
        y: number;
        width: number;
        height: number;
    }, textureDimension: {
        width: number;
        height: number;
    }, flipped: boolean, textureTarget?: SyphonTextureTarget): void;
    publish(textureId: number, width: number, height: number, textureTarget?: SyphonTextureTarget, flipped?: boolean): void;
    publishBoundTexture(width: number, height: number, textureTarget?: SyphonTextureTarget, flipped?: boolean): void;
    get name(): string;
    get description(): SyphonServerDescription;
    get hasClients(): boolean;
}
