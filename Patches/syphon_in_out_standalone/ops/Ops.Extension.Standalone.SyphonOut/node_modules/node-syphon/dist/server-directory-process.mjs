var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => {
  __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
  return value;
};
import bindings from "bindings";
const SyphonServerDescriptionNameKey = "SyphonServerDescriptionNameKey";
const SyphonServerDescriptionAppNameKey = "SyphonServerDescriptionAppNameKey";
const SyphonServerDescriptionUUIDKey = "SyphonServerDescriptionUUIDKey";
const SyphonAddon = bindings({
  bindings: "syphon",
  // The bin folder from the lib one.
  try: [
    // For standard installation.
    ["module_root", "node_modules", "node-syphon", "dist", "bin", "syphon.node"],
    // For local examples (monorepo).
    ["module_root", "dist", "bin", "syphon.node"]
  ]
});
const _SyphonServerDirectorySingleton = class _SyphonServerDirectorySingleton {
  constructor() {
    __publicField(this, "_directory");
    if (_SyphonServerDirectorySingleton._instance) {
      return _SyphonServerDirectorySingleton._instance;
    }
    _SyphonServerDirectorySingleton._instance = this;
    this._directory = new SyphonAddon.ServerDirectory();
    this._directory.listen();
  }
};
__publicField(_SyphonServerDirectorySingleton, "_instance");
let SyphonServerDirectorySingleton = _SyphonServerDirectorySingleton;
const instance = new SyphonServerDirectorySingleton();
Object.freeze(instance);
export {
  SyphonServerDescriptionAppNameKey,
  SyphonServerDescriptionNameKey,
  SyphonServerDescriptionUUIDKey,
  instance as SyphonServerDirectory
};
