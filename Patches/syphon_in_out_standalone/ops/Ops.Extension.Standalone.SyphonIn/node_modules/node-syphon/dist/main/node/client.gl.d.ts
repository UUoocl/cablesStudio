import { SyphonFrameData } from '../universal';
import { SyphonServerDescription } from '../../common/types';

export interface SyphonOpenGLClientConstructorOptions {
    server: SyphonServerDescription;
    handle?: Buffer;
}
export type { SyphonFrameData };
export declare class SyphonOpenGLClient {
    private client;
    private listeners;
    private isFrameListenerSet;
    constructor(description: SyphonServerDescription);
    dispose(): void;
    on(channel: string, callback: (frame: SyphonFrameData) => void): void;
    off(channel: string, callback: (frame: SyphonFrameData) => void): void;
    private frameDataListenerCallback;
}
